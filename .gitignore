bin/
obj/
*.user
.vs/
TestResults/
*.nupkg
# Screenshots are ignored by default — several were taken on a real prospect's install
# (cabp) and must never reach the public repo. Whitelist vetted images explicitly.
docs/*.png
!docs/security-viewer-overview.png
